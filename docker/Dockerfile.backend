FROM python:3.12-slim

WORKDIR /app

RUN apt-get update && apt-get install -y --no-install-recommends \
    gcc libpq-dev && \
    rm -rf /var/lib/apt/lists/*

COPY pyproject.toml README.md project.py ./
COPY src/ ./src/

RUN pip install --no-cache-dir -e ".[agent]"

COPY docs/kb/ ./docs/kb/

WORKDIR /app

EXPOSE 8000

CMD ["python", "project.py", "--host", "0.0.0.0", "--port", "8000"]
