# RelayOps - Production dependencies for CML deployment
# Generated from actual imports in src/ (excluding mock_services/ and tests)

# Web framework & ASGI server
fastapi>=0.110
uvicorn>=0.27
# uvicorn 0.31+ made click an optional extra but config.py still imports it
# at module load. Pin it explicitly so backend import works on minimal envs.
click>=8.0
gunicorn>=21.2
starlette>=0.36

# Data validation
pydantic>=2.0

# Database (SQLAlchemy 2.x + PostgreSQL/YugabyteDB driver)
sqlalchemy>=2.0
psycopg2-binary>=2.9

# Auth
PyJWT>=2.8

# Scheduling (cron parsing — Controller missed-run checks, onboarding validation)
croniter>=2.0

# File uploads (FastAPI multipart forms — onboarding document ingest)
python-multipart>=0.0.9

# HTTP client (sync paths in integrations that predate httpx)
requests>=2.31

# Config & logging
PyYAML>=6.0
loguru>=0.7

# Metrics (Prometheus /metrics exposition)
prometheus-client>=0.19.0

# HTTP client (used by integrations/control_interface, checker)
httpx>=0.27